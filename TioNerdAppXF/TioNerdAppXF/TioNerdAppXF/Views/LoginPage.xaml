<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TioNerdAppXF.Views.LoginPage"
             Title="{Binding Title}"
             BackgroundColor="#e0e0e0">

    <StackLayout HorizontalOptions="CenterAndExpand"
                 VerticalOptions="CenterAndExpand">
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#000000" />

        <Label Text="Entre no aplicativo com seu Facebook" Style="{StaticResource StyleDescription}" />
        <Image Source="ic_open.png"
               HorizontalOptions="Center"
               VerticalOptions="Center">
            <Image.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding LoginCommand}" />
            </Image.GestureRecognizers>
        </Image>

    </StackLayout>
</ContentPage>