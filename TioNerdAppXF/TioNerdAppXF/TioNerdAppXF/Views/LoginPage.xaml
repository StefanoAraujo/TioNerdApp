<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TioNerdAppXF.Views.LoginPage"
             Title="{Binding Title}"
             BackgroundColor="#e0e0e0">

    <StackLayout HorizontalOptions="CenterAndExpand"
                     VerticalOptions="CenterAndExpand">
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#000000" />

        <Label Text="Entre no aplicativo com seu Facebook" TextColor="#000000" FontSize="Large" HorizontalTextAlignment="Center" />

        <Button Text="Facebook Login" 
                TextColor="#FFFFFF" 
                FontSize="Large" 
                BackgroundColor="#3b5998" 
                Command="{Binding LoginCommand}" 
                Image="ic_open.png"
                VerticalOptions="CenterAndExpand"/>

    </StackLayout>

</ContentPage>