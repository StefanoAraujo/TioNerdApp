<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TioNerdAppXF"
             x:Class="TioNerdAppXF.MainPage"
             Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Sair" Command="{Binding LogOutCommand}"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout Padding="60"
                     HorizontalOptions="Center"
                     VerticalOptions="CenterAndExpand"
                     BackgroundColor="#e0e0e0">
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#000000" />
            <Label Text="Id do usuário" FontSize="Medium" TextColor="#000000" />
            <Label Text="{Binding UserId}" FontSize="Large" TextColor="#3b5998" />

            <Label Text="Id do usuário" FontSize="Medium" TextColor="#000000" />
            <Label Text="{Binding Token}" FontSize="Large" TextColor="#3b5998" />

            <Switch IsToggled="{Binding IsLogged}" />
        </StackLayout>
    </ScrollView>

</ContentPage>